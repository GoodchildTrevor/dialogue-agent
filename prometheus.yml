global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: dialogue-agent
    static_configs:
      - targets:
          - app:8000        # Docker service name
    metrics_path: /metrics
    scheme: http